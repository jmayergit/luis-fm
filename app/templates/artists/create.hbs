<form {{action "create" model on="submit"}}>
  <div class="item">
    {{input type="text" value="name"}}
  </div>
  <div class="item">
    {{input type="submit" value="Submit"}}
  </div>
</form>
{{outlet}}
